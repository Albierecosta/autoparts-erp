{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo '=== BOOT ===' && bundle exec rails db:migrate 2>&1 && echo '=== SERVER ===' && bundle exec rails server -p ${PORT:-3000} -b 0.0.0.0",
    "healthcheckPath": "/ping",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
