<% content_for :title, "Nova Venda" %>

<div class="mb-4">
  <nav class="flex items-center gap-2 text-sm text-gray-500 dark:text-gray-400 mb-2">
    <%= link_to "Vendas", sales_path, class: "hover:text-gray-700 dark:hover:text-gray-200" %>
    <span>/</span>
    <span class="text-gray-900 dark:text-white">Nova Venda</span>
  </nav>
  <h1 class="text-2xl font-bold text-gray-900 dark:text-white">
    <%= @sale.quote? ? "Novo Orçamento" : "Nova Venda" %>
  </h1>
</div>

<%= render "form", sale: @sale %>
