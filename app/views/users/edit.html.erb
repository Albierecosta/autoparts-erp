<% content_for :title, "Editar #{@user.name}" %>
<div class="mb-6">
  <nav class="flex items-center gap-2 text-sm text-gray-500 dark:text-gray-400 mb-2">
    <%= link_to "Usuários", users_path, class: "hover:text-gray-700 dark:hover:text-gray-200" %><span>/</span>
    <span class="text-gray-900 dark:text-white"><%= @user.name %></span>
  </nav>
  <h1 class="text-2xl font-bold text-gray-900 dark:text-white">Editar Usuário</h1>
</div>
<%= render "form", user: @user %>
